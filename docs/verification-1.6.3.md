# 1.6.3 验证记录

日期：2026-09-17。环境：macOS arm64。

## 范围

本次纳入此前工作区剩余的安全存储、下载限制、主题、EPUB 处理、
去重实现、OpenCC 资源、测试及发布流程改动，并复查后补充修复。
1.6.2 的复杂字体与 epubx 修复已通过 PR 合入主分支，本次保留其历史。
应用版本与应用内显示同步为 `1.6.3+31`。

原书、正文、原字体、凭据和生成的 EPUB 不提交。
`测试文件/` 与 `build/` 中的夹具、报告、日志及产物仅在本机保留。

## 自动检查

- 最终完整 `flutter test --reporter expanded`：266 项通过、20 项跳过、0 失败。
- 跳过项需要额外本机夹具或显式路径，不计为已验证；指定 C43 输入的流程另行串行执行。
- 最终 `dart analyze lib`：无问题。
- 发布与 CI YAML 解析、串行入口 Bash 语法、`git diff --check` 通过。
- `flutter build macos --release` 成功。HarfBuzz framework 含 arm64/x86_64，
  `otool -L` 显示仅依赖系统 libc++/libSystem，无 Homebrew 运行库依赖。
- 构建仍提示部分插件未支持 Swift Package Manager，以及 native asset 跨架构
  framework 命名警告；实际合并后的 HarfBuzz 双架构库已检查，未将这些警告隐瞒为零告警。

## 真实书籍

输入为 C43《王立群读史记》手机 EPUB，34,054,305 字节，
228 个正文文档、60 张图片、2 款字体。

输入 SHA256：
`0d3efd42e086ac51037653126e2c86154a5a9ea42160a9754295b9e1d7050b4a`。
运行后再次计算一致，原书未改动。

执行：

```bash
FLUTTER_BIN=/Users/aaa/flutter/bin/flutter \
bash tool/verify_real_epub.sh /absolute/path/book.epub build/review-1.6.3-verified
```

- 主流程 32 项：30 项直接通过，2 项标记 `NOT_EXERCISED`。
- 原书无远程图片、无待转换的标准脚注；使用派生副本分别补充远程图片本地化和脚注回转测试，均通过。
- 补充流程 7 项通过：阅读器导入、全文差异复制还原、WiFi 本机下载及 Range 字节一致、离线微信读书想法注入、脚注回转、名称混淆/还原的全文哈希一致、受控图片下载。
- 最后重新严格导入 31 份 EPUB，0 失败。
- 独立 Python 审计 31 份 EPUB：XML/ZIP/manifest/spine 错误 0，资源/锚点警告 0；图片成功解码 1,794 张次（含重复出现在不同产物中的图片）。
- CLI 使用真实加密副本成功生成中文路径输出；参数错误、已有输出保护、无 OPF 不误报成功另有自动测试。

最初曾将全部测试文件与同一个输出环境变量一起运行，导致依赖产物的测试
先于生成流程执行，出现 7 个失败。这次运行不计为验收通过。
新增串行入口后，在全新目录按生成、补测、导入的顺序全部通过。

## 字体

| 项目 | 原始字节数 | 精简后字节数 |
| --- | ---: | ---: |
| EPUB | 34,054,305 | 26,401,085 |
| 思源宋体 CFF/OTF | 11,257,480 | 1,678,476 |
| 楷体 GSUB/TTF | 4,785,912 | 4,729,700 |

字体覆盖、cmap 引用、表校验和检查通过。两款字体累计 916 次全书及保留字符集
横排/竖排比较，实际字形轮廓、cluster、advance、offset 无差异。
整体减少 22.47%；楷体仅缩减约 1.17%，不宣称所有字体均能大幅缩小。
fontTools/uharfbuzz 仅为开发审计依赖，应用运行无需 Python 或 Homebrew。

## 复查修复

- 输出权限探测使用独立临时子目录，不覆盖已有 `.write_test`，并验证并发调用和清理。
- 拆分返回实际生成路径，后台 isolate 向 UI 传递结构化结果；旧分卷不计入本次数量或复制到公共目录，无输出按失败处理。
- 主题加载不覆盖用户的新选择，不在销毁后触发通知。
- 微信读书页面关闭 HTTP 客户端，安全存储写入失败可见，迁移失败保留唯一旧副本。
- OpenCC 测试覆盖 gzip 加载、词组/单字联合最长匹配、恒等词组与扩展字符；说明快照包含内容更新而非仅压缩。

## 边界

- 本地网络传输不等于跨设备或公网环境验证。
- 微信读书注入及网络图片测试使用离线/受控响应；未使用真实账号验证在线登录、验证码或 SMTP 邮件投递。
- 未在 Kindle、Apple Books 等外部阅读器进行人工阅读验收。
- Android/iOS/Windows/Linux 构建和实机安全存储仍需各平台验证，不将 macOS 结果外推到全平台。
- 完整 `dart analyze lib test tool` 另报告 17 个 warning 和 124 个 info，主要来自既有测试/辅助脚本；无 error。应用源码 `dart analyze lib` 为零问题。
- OpenCC 快照未记录上游提交号；保留来源不确定性说明，不虚构版本。
- 本次只推送源码到主分支，不创建 tag，不声称已经发布多平台安装包。
